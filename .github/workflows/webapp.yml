name: Build and deploy Python app to Azure Web App - IDSM41P

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            .
            !antenv/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          - name: Login to Azure
  uses: azure/login@v2
  with:
        client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_81CC5204F4D84D008A04620F1E5FD630 }}
        tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_304C2084B5BC40E49A13CF01BF80BC56 }}
        subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_84691E04FFD54461ACB044E999FA0382 }}


      - name: Test secrets
        run: |
          echo "Client ID length: ${#AZURE_CLIENT_ID}"
          echo "Tenant ID length: ${#AZURE_TENANT_ID}"
          echo "Subscription ID length: ${#AZURE_SUBSCRIPTION_ID}"

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'IDSM41P'
          slot-name: 'Production'
          package: .
